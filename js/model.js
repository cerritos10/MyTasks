export default class Model {
    constructor() {
        this.view = null;
        this.tasks = [];
    }

   /*  setView(view) {
        this.view = view;
    } */

    /* getTask(tasks) {
        return this.tasks;
    }  */

    addTaskModel(title, description, fecha) {
        console.log(title, description, fecha);
    }
}